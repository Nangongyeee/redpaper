# 2026-08 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 234 篇论文 / 视频；2026-08-19_

**一句话：** 世界模型与VLA融合，迈向全身智能与安全

## 一句话
世界模型与VLA融合，迈向全身智能与安全

## 五大主题

### 世界模型成为机器人学习核心
本月世界模型相关论文数量激增，从通用世界建模到动作条件预测，成为机器人学习的新焦点。`Hydra-0` 将动作表示为像素运动，实现通用世界建模与控制，误差大幅降低；`AlayaWorld` 通过流式 3D 点缓存和潜在条件对齐，提升长时程世界建模的交互性。`DreamX-Phi 1.0` 提出动作条件视频世界模型，通过几何编码和深度分支提升机器人操作预测的忠实性。`JEPA-WAM` 通过阶段级预测提升机器人操作成功率并减少执行步数。这些工作表明，世界模型正从单纯预测未来帧转向与动作生成深度融合，为机器人提供更丰富的环境先验。

### VLA 策略优化与泛化
视觉-语言-动作模型（VLA）的优化与泛化仍是核心议题。`Prism-GRPO` 通过拆分同结果组，在VLA策略优化中减少最多56%的rollout并提升成功率；`时间GRPO` 通过阶段级信用分配改进VLA强化学习，提升任务成功率与样本效率。`StellaVLA` 通过结构化演示在测试时适应，提升VLA模型泛化能力；`MatchingPolicy` 通过对应感知扩散策略解耦匹配与学习，实现跨物体少样本泛化。`G0.5` 用单一自回归流统一推理与动作，在7个基准上超越SOTA。这些工作从训练策略、数据利用和模型架构多维度推动VLA性能提升。

### 人形全身控制与移动操作
人形机器人的全身控制与移动操作取得显著进展。`HAF` 通过层级动作流和潜在RL将通用VLA适配到人形全身移动操作，提升协调性；`$ω$-0` 实现人形机器人并发移动操作，在11个家庭任务上优于现有基线。`LUCID` 通过想象动力学规划可复用技能，提升长时程人形移动操作任务成功率。`RoboStriker` 通过潜空间博弈实现人形拳击，减少平衡失败并提升战术性能。`StableMimic` 通过分布外训练和专家混合，实现人形机器人跌倒后自主恢复，跟踪误差最低。这些工作展示了人形机器人在复杂动态任务中的潜力。

### 安全、鲁棒性与不确定性
随着机器人进入真实世界，安全与鲁棒性成为关注重点。`MANIGUARD` 基准评估机器人操作安全，发现成功任务中 6-21% 违规；`UniTexture` 通过单一纹理攻击多任务VLA，成功率从90%降至48.4%；`DRIFT` 通过攻击第一步去噪，以单补丁破坏流匹配VLA几乎所有任务。防御方面，`SARF` 提出结构感知的鲁棒微调，防御物理注意力劫持；`ContactGuard` 在接触前预测失败并中止，提升操作安全性。`G2SD` 通过图引导扩散规划，将无碰撞到达率从40-50%提升至98%。这些工作揭示了VLA策略的脆弱性，并提出了初步的防御与安全机制。

### 数据效率与仿真到现实迁移
数据效率与仿真到现实迁移仍是瓶颈。`Ego2Robot` 将第一人称人类视频转化为机器人训练数据，生成最大规模数据集，提升泛化能力；`SiMDex` 通过相似性挖掘从海量人类视频中精选数据，提升灵巧操作成功率。`R2S-EGO` 用双代理细化稀疏采集，提升真实到模拟的渲染与策略成功率。`Open-DiffLoco` 利用可微分模拟训练盲四足运动策略，实现硬件部署。`通过模拟器侧动力学归一化` 显著缩小并联腿机构的仿真到现实差距。这些工作从数据生成和算法层面缓解数据稀缺问题。

## 值得抄作业的工作

- `LiLa-WAM`：单GPU可训练的轻量级世界-动作模型，在50个任务上达到90.48%成功率，值得抄其轻量化架构与训练策略。
- `Twin`：通过测试时数字孪生，让编码智能体自动构建世界模型，在ARC-AGI-3游戏中达到97.8%的完成率，值得抄其测试时构建世界模型的方法。
- `RTCF`：无需训练，通过分离检索与传输，提升冻结VLA策略在长时程操作中的成功率，值得抄其即插即用的测试时增强策略。
- `XPolicyLab`：统一标准将机器人策略集成成本从O(NM)降至O(N+M)，集成时间从5小时减至30分钟，值得抄其标准化集成框架。
- `GraspMeanFlow`：SE(3)-等变MeanFlow，单步生成6-DoF抓取，性能媲美五步迭代流模型，值得抄其等变生成模型的效率设计。

## 还没解决的事

- 世界模型的物理校准问题：`CaliBench` 和 `GAUGE` 均发现多数世界模型物理校准不佳，如何提升物理一致性仍是难题。
- VLA策略的安全漏洞：`MANIGUARD` 和 `DRIFT` 显示VLA策略易受攻击且存在违规行为，防御机制尚不完善。
- 跨形态泛化：`XEWorld` 发现当前世界模型仅按视觉相似性泛化，无法处理新机器人形态，跨形态迁移仍待突破。
- 长时程任务的可靠性：`PlayWorld` 显示现有世界模型在长时程交互目标上不可靠，如何保持长期一致性是挑战。
- 数据效率的极限：尽管有 `Ego2Robot` 等数据生成方法，但真实世界数据获取成本仍高，如何进一步降低数据需求是未解问题。