# 2026-08 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 143 篇论文 / 视频；2026-08-13_

**一句话：** 世界模型与VLA深度融合，具身智能加速落地

## 一句话
世界模型与VLA深度融合，具身智能加速落地

## 五大主题
### 世界模型成为具身智能核心引擎
本月世界模型相关论文爆发，从预测未来到规划控制，成为具身智能的核心引擎。`JEPA-WAM` 通过联合嵌入预测与动作生成耦合，在LIBERO-Plus上取得最佳性能；`Flex-π` 利用共享VAE同时预测RGB、3D点图和语义，实现高效多流世界-动作模型；`LiLa-WAM` 以轻量级潜在推理在50个任务上达到90.48%成功率。`WorldCycle` 通过可逆动作循环实现自验证强化学习，显著减少长时程视频世界模型的漂移。`Faster-WAM` 则挑战深层动作模块的必要性，用轻量动作头实现低延迟高性能。这些工作表明世界模型正从单纯预测走向行动决策，成为VLA策略的“想象引擎”。

### VLA策略泛化与鲁棒性提升
视觉-语言-动作模型（VLA）的泛化与鲁棒性成为焦点。`StellaVLA` 通过结构化演示在测试时适应，提升VLA模型泛化能力；`CrossTracer` 通过轨迹残差自适应实现跨形态导航，在NaviTrace基准上超越Gemini-2.5-Pro。`DyPES-VLA` 学习共享动力学先验与具身特定MoE动作头，实现跨具身操作。`Mind-VLA` 通过指令感知对齐目标物体3D特征，提升遮挡和精细操作表现。`AtVLA` 利用寄存器token和局部细化，提升空间操作精度。`SARF` 防御物理注意力劫持，`DRIFT` 则揭示流匹配VLA的脆弱性，攻击第一步去噪即可破坏几乎所有任务。这些工作共同推动VLA从实验室走向真实世界。

### 数据规模与质量双轮驱动
数据是具身智能的燃料，本月在数据规模与质量上均有突破。`Ego2Robot` 将第一人称人类视频转化为机器人训练数据，生成最大规模数据集，提升泛化能力；`SiMDex` 通过相似性挖掘从海量人类视频中精选数据，提升灵巧操作成功率；`HandEdit` 提供首个将人类手部图像转换为灵巧机器人具身的大规模基准，含2亿实例。`JoyAI-RA 0.5` 通过双动作对齐利用人类视频扩展机器人操作学习，性能随数据量持续提升。`DynamicManip` 从单个静态演示生成动态操作数据，实现高效低延迟的动态操作。数据来源从遥操作扩展到人类视频、仿真生成，规模与多样性显著提升。

### 移动操作与全身控制走向成熟
人形机器人的移动操作与全身控制成为热点。`MobileWAM` 将世界行动模型扩展至移动操作，通过预见链实现全身控制并超越SOTA；`LUCID` 通过想象动力学规划可复用技能，提升长时程人形移动操作任务成功率；`PanoVLA` 利用全景感知提升移动操作的空间理解，成功率显著优于局部视图基线。`ω-0` 模型实现人形机器人并发移动操作，在11个家庭任务上优于现有基线。`StableMimic` 通过分布外训练和专家混合，实现人形机器人跌倒后自主恢复，跟踪误差最低。`Light-Loco-Parkour` 仅用深度和速度指令实现人形全身跑酷，无需参考或技能标签。全身控制从单一技能走向复杂场景，移动操作从规划走向执行。

### 安全、校准与评估体系初步建立
随着系统复杂度提升，安全、校准与评估成为关键议题。`WorldExam` 新基准评估世界模型，发现高视觉质量不保证内在反应性；`GAUGE` 基准揭示物理引擎和视频模型均存在物理保真度缺陷；`WorldSimProbe` 诊断动作条件世界模型的模拟器保真度，发现系统性退化。`CaPTURe` 基于共形预测的几何感知算法，校准接触感知的不确定性，提升任务成功率。`GUARD` 通过消融KV缓存检测扩散VLA的弱相关性，提升未见任务失败检测。`AutoIntervene` 通过校准干预阈值，在动作分块策略与操作员间智能切换，提升任务成功率并减少人工控制时间。这些工作为具身智能的可靠部署奠定基础。

## 值得抄作业的工作
- `LiLa-WAM`：单GPU可训练，50个任务90.48%成功率，值得抄其轻量潜在推理架构。
- `Ego2Robot`：将第一人称人类视频转化为机器人训练数据，值得抄其数据合成流程。
- `Faster-WAM`：轻量动作头实现低延迟高性能，值得抄其动作模块设计。
- `CaPTURe`：共形预测校准接触感知不确定性，值得抄其不确定性量化方法。
- `StableMimic`：分布外训练和专家混合实现跌倒恢复，值得抄其鲁棒运动控制策略。

## 还没解决的事
- 世界模型的物理保真度仍不足，`GAUGE` 和 `WorldSimProbe` 均揭示系统性缺陷，如何提升物理一致性尚无定论。
- VLA策略的对抗鲁棒性脆弱，`DRIFT` 单补丁即可破坏几乎所有任务，防御机制尚不完善。
- 跨形态泛化仍受限，`XEWorld` 显示当前世界模型仅按视觉相似性泛化，无法处理新机器人形态。
- 长时程任务的记忆与推理仍不成熟，`WorldCycle` 虽减少漂移，但复杂场景下的长期一致性未解决。
- 数据效率与真实世界部署差距大，`R2S-EGO` 等虽提升sim2real，但真实世界在线学习仍样本效率低。