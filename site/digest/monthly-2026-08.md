# 2026-08 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 109 篇论文 / 视频；2026-08-11_

**一句话：** 世界模型与VLA深度融合，迈向全身智能

## 一句话
世界模型与VLA深度融合，迈向全身智能。

## 五大主题

### 世界模型成为VLA的“第二大脑”
本月世界模型不再只是视觉预测工具，而是深度嵌入VLA策略的训练与推理流程。`世界令牌`在训练时引入世界建模，部署时零额外成本；`JEPA-WAM`将潜在转移预测与动作生成耦合，在LIBERO-Plus上取得最佳性能。`DreamWAM`进一步超越RGB预测，直接预测动作相关的未来状态，提升鲁棒性。`Faster-WAM`则挑战深层动作模块的必要性，用轻量动作头实现低延迟，表明世界模型与动作模块的耦合方式正在被重新审视。

### 长时程操作：记忆与分层成为关键
长时程任务不再是简单堆叠步数，而是需要显式记忆与分层架构。`BridgeVLA++`通过统一时空记忆增强3D操作，在记忆依赖基准上达到最优；`显式语言记忆模块`直接提升VLA在长时程任务上的成功率。`HiRoC`提出分层后训练，解耦规划与执行；`ChainVLA`用统一执行状态链接查询，实现长时程高成功率。`GORDON`则从视频学习对象中心奖励，自动分解长时程任务，平均成功率74.4%。

### 数据规模与效率：从人类视频到合成数据
数据获取从遥操作转向人类视频与合成数据。`Ego2Robot`将第一人称人类视频转化为机器人数据，生成最大规模数据集；`SiMDex`通过相似性挖掘从海量人类视频中精选灵巧操作数据；`JoyAI-RA 0.5`用双动作对齐利用人类视频扩展学习，性能随数据量持续提升。`RoboReact`从单张RGB-D图像生成视频并蒸馏技能，无需遥操作。`DynamicManip`从单个静态演示生成动态操作数据，大幅降低数据采集成本。

### 全身控制与移动操作：从“手”到“全身”
人形机器人不再局限于桌面操作，全身移动操作成为焦点。`$\omega$-0`实现人形并发移动操作，在11个家庭任务上优于基线；`MobileWAM`通过预见链将世界行动模型扩展至移动操作，实现全身控制；`PanoVLA`用全景感知提升移动操作的空间理解。`Light-Loco-Parkour`仅用深度和速度指令实现人形全身跑酷；`StableMimic`实现跌倒后自主恢复。`LooperMuscle`用45分钟训练达到接近PPO的全身跟踪精度。

### 安全、鲁棒性与评测：从“能跑”到“可信”
随着VLA部署，安全性评测与对抗攻击成为热点。`DRIFT`通过单补丁攻击使流匹配VLA几乎所有任务失败；`SARF`防御物理注意力劫持，降低失败率；`GUARD`基于不确定性检测未见任务失败。`WorldExam`基准发现高视觉质量不保证内在反应性；`GAUGE`揭示物理引擎和视频模型均存在物理保真度缺陷；`XEWorld`指出当前世界模型无法泛化到新机器人形态。`GST-Bench`显示最强VLM在全局空间感知上仅得42.68，远低于人类79.08。

## 值得抄作业的工作

- `LiLa-WAM`：单GPU可训练，50任务90.48%成功率，值得复现其轻量架构设计。
- `AutoIntervene`：校准干预阈值，在动作分块策略与操作员间智能切换，简单有效，值得在真实系统上验证。
- `Open-DiffLoco`：开源可微分学习框架，实现盲四足运动策略硬件部署，值得借鉴其sim2real流程。
- `WorldCycle`：利用可逆动作循环自验证强化学习，显著减少长时程视频世界模型漂移，值得在视频预测任务中尝试。
- `ReTouch`：在线细化触觉预测，提升接触丰富灵巧操作成功率，值得在触觉反馈回路中集成。

## 还没解决的事

- 世界模型对新机器人形态的泛化仍失败（`XEWorld`），如何实现跨形态世界模型仍是开放问题。
- 物理保真度评测（`GAUGE`）显示仿真引擎和视频模型均有缺陷，但如何自动修正这些缺陷尚无方案。
- VLA策略对指令改写脆弱（`GSR`），语义重绑定仅用规范演示提升鲁棒性，但复杂指令组合仍不可靠。
- 长时程任务中记忆机制（`BridgeVLA++`、`显式语言记忆`）虽有效，但记忆容量与遗忘问题未解决。
- 对抗攻击（`DRIFT`）显示流匹配VLA极易被单补丁破坏，防御方法（`SARF`）仅针对特定攻击，通用防御缺失。