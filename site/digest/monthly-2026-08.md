# 2026-08 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 92 篇论文 / 视频；2026-08-10_

**一句话：** 世界模型与VLA深度融合，迈向长时程具身智能

## 一句话
世界模型与VLA深度融合，迈向长时程具身智能。

## 五大主题
### 世界模型成为VLA的“第二大脑”
本月世界模型不再只是视频预测玩具，而是深度嵌入VLA策略。`$\omega$-0` 提出潜在预测世界动作模型，实现人形机器人并发移动操作，在11个家庭任务上领先；`MobileWAM` 将世界行动模型扩展至移动操作，通过预见链实现全身控制；`DreamWAM` 则超越RGB预测，直接预测动作相关未来状态，提升鲁棒性。`Faster-WAM` 挑战深层动作模块必要性，用轻量动作头实现低延迟，暗示世界模型与动作模块的解耦趋势。`LiLa-WAM` 在单GPU上训练轻量级世界-动作模型，50个任务达90.48%成功率，证明世界模型可以“小而美”。

### 长时程任务：从“分块”到“分层”与“记忆”
长时程操作成为焦点，动作分块（Action Chunking）被重新审视。`为什么动作分块能提升行为克隆性能？` 揭示其成功源于隐式集成效应，而非传统假设。`AutoIntervene` 通过校准干预阈值，在动作分块策略与操作员间智能切换，提升成功率并减少人工控制。`ChainVLA` 用统一执行状态链接查询，实现长时程操作；`显式语言记忆用于长时程规划` 为VLA添加语言记忆模块；`BridgeVLA++` 用统一时空记忆增强3D操作。`GORDON` 从视频学习对象中心奖励，自动分解长时程任务，成功率74.4%。`BCP` 用伯努利延续策略学习自适应执行视界，决定继续或重新规划。

### 数据效率革命：从人类视频到单演示生成
数据获取方式迎来突破。`Ego2Robot` 将第一人称人类视频转化为机器人训练数据，生成最大规模数据集；`JoyAI-RA 0.5` 用双动作对齐框架，利用人类视频扩展操作学习；`SiMDex` 从海量人类视频中挖掘相似自我中心视频，提升灵巧操作；`DynamicManip` 从单个静态演示生成动态操作数据；`RoboReact` 从单张RGB-D图像生成视频并蒸馏全身操作技能，无需遥操作。`C2Dex` 从单目视频重建接触并迁移灵巧操作。这些工作大幅降低数据采集成本，让“少样本”甚至“零样本”成为可能。

### 鲁棒性与安全：对抗攻击与防御成新战场
随着VLA部署到真机，鲁棒性与安全性问题凸显。`DRIFT` 通过对抗性补丁攻击第一步去噪，破坏流匹配VLA几乎所有任务；`SARF` 提出结构感知鲁棒微调，防御物理注意力劫持；`GUARD` 用不确定性检测扩散VLA的失败；`任务向量否定` 研究发现任务向量减法在VLA中脆弱且局部性差。`跨视角动作一致性` 提升对相机移动的鲁棒性；`基于语义重绑定` 增强指令泛化。这些工作表明，VLA不仅需要“会做”，还要“抗干扰”。

### 新形态与新硬件：从四足到人形，从触觉到全身
机器人形态与硬件创新加速。`StableMimic` 实现人形跌倒后自主恢复；`Light-Loco-Parkour` 仅用深度和速度指令实现人形全身跑酷；`LooperMuscle` 用结构化专家混合实现快速稳定的人形全身跟踪。`KILVO` 融合多传感器实现人形高精度里程计；`几何可扩展全身触觉` 用3D打印共形EIT皮肤实现全身触觉。`R2S-EGO` 用双代理细化稀疏采集，提升真实到模拟的渲染与策略成功率。`Open-DiffLoco` 开源可微分学习框架训练盲四足运动策略。这些工作推动机器人从实验室走向复杂真实环境。

## 值得抄作业的工作
- `LiLa-WAM`：单GPU可训练的世界-动作模型，90%+成功率，值得复现其轻量化架构设计。
- `AutoIntervene`：动作分块策略的校准干预，简单有效，值得在现有BC策略上快速验证。
- `为什么动作分块能提升行为克隆性能？`：理论分析揭示隐式集成效应，值得深入理解并指导分块长度选择。
- `DynamicManip`：从单个静态演示生成动态操作数据，数据效率极高，值得借鉴其数据生成流程。
- `Ego2Robot`：将人类视频转化为机器人数据，规模大且泛化好，值得用于预训练或数据增强。

## 还没解决的事
- 世界模型的物理保真度仍不足：`GAUGE` 基准显示物理引擎和视频模型均有缺陷，`WorldExam` 发现高视觉质量不保证内在反应性，如何构建物理一致的世界模型仍是难题。
- 跨形态泛化有限：`XEWorld` 显示世界模型仅按视觉相似性泛化，无法处理新形态，如何实现真正的形态无关表示？
- 长时程任务中的错误累积与恢复：尽管有记忆和分层方法，但长时程操作中错误传播问题未根本解决，`WorldCycle` 尝试自验证但仅限视频世界模型。
- VLA的对抗鲁棒性脆弱：`DRIFT` 单补丁即可破坏几乎所有任务，防御方法如`SARF` 仅针对特定攻击，通用防御框架缺失。
- 触觉与力反馈的集成：`ReTouch` 和`VIDP` 展示了触觉预测和可变阻抗的潜力，但全身触觉皮肤与VLA的深度融合仍未见成熟方案。