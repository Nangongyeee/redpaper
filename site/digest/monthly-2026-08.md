# 2026-08 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 390 篇论文 / 视频；2026-08-28_

**一句话：** 世界模型成主线，VLA 走向全身与长时程

## 一句话
世界模型从预测工具升级为控制核心，VLA 向全身、长时程与安全演进。

## 五大主题
### 世界模型成为控制核心
本月世界模型不再只是视频预测器，而是直接参与决策与控制。`GigaBrain-WBC-0.5` 首次将行为世界模型用于人形全身控制，`Riemann-1.0` 统一世界与动作建模并在真机达到 SOTA。`LeFlow` 用生成式潜在轨迹替代迭代优化，`LAWA` 用潜在动作实现高效未来想象。`JEPA-WAM` 通过阶段级预测提升操作成功率，`WorldCycle` 用可逆动作循环做自验证强化学习减少漂移。世界模型正从“看得准”转向“控得好”。

### VLA 走向全身与长时程
VLA 模型不再局限于桌面操作，开始向全身移动操作和长时程任务扩展。`HAF` 通过层级动作流将通用 VLA 适配到人形全身移动操作，`ω-0` 实现人形并发移动操作，`MobileWAM` 用预见链弥合世界模型与移动操作。长时程方面，`TemporalFlow-VLA` 学习物理 grounded 执行历史，`BATON` 用子任务探索将成本从指数降为线性，`ChainVLA` 通过统一执行状态链接查询。`SparkVLA` 用停止感知分层解决动作长度依赖。

### 触觉与力反馈成为刚需
接触丰富的操作离不开触觉，本月多篇工作将触觉信号融入策略。`TacForcing` 用执行时触觉反馈流式生成动作，`HiTac-WAM` 分层触觉预测将成功率从 31.1% 提升至 61.1%。`VISTA` 用视觉变形场替代触觉，`ReTouch` 在线细化触觉预测。`ViHaTeleop` 低成本视觉触觉遥操作系统，`缺失的触觉` 证明空间分布触觉反馈减少轨迹偏差 29%-79%。触觉正在从“可选”变成“必备”。

### 数据规模与质量双轮驱动
数据仍是瓶颈，但重点从“更多”转向“更有效”。`RoboEdit` 将人类视频转化为机器人经验，`Ego2Robot` 从第一人称数据合成机器人数据，`SiMDex` 挖掘相似自我中心视频。`Zero-WAM` 用人类视频作为上下文实现零样本泛化，`JoyAI-RA` 用双动作对齐扩展学习。`PRISM` 提供大规模工业装配多模态数据集，`LabDex` 统一真实与仿真基准。数据质量方面，`InstructMove` 防止视觉捷径，`SoftVTBench` 揭示触觉并非总是有效。

### 安全与鲁棒性受到重视
随着 VLA 走向实际部署，安全与鲁棒性成为焦点。`CertVLA` 提供首个针对物理攻击的认证防御，`UniTexture` 用单一纹理将 VLA 成功率从 90% 降至 48.4%，`DRIFT` 用单补丁破坏流匹配 VLA。`MANIGUARD` 基准发现成功任务中 6-21% 违规，`LIBERO-VIFO` 评估视觉线索跟随安全。`ContactGuard` 在接触前预测失败并中止，`结构感知的鲁棒微调` 防御物理注意力劫持。安全从“事后修补”走向“事前认证”。

## 值得抄作业的工作
- `PredVLA`：0.68M 参数达到高成功率，证明小模型也能做好 VLA，值得抄其架构设计。
- `GRAFT`：区域监督和缓存重用实现高效在线适应，成功率提升 25 个百分点，值得抄其适应机制。
- `TacForcing`：流式动作生成结合触觉反馈，简单有效，值得抄其触觉融合方式。
- `WorldCycle`：可逆动作循环做自验证强化学习，减少长时程漂移，值得抄其自监督信号设计。
- `XPolicyLab`：统一标准将策略集成成本从 O(NM) 降至 O(N+M)，值得抄其工程化思路。

## 还没解决的事
- 世界模型对非专家动作的遵循能力仍不足，`WorldEcho` 诊断显示动作条件生成与真实执行有差距。
- 跨具身泛化远未解决，`XEWorld` 发现世界模型仅按视觉相似性泛化，无法处理新形态。
- 长时程记忆仍是短板，`PlayWorld` 显示现有模型在长时程交互目标上不可靠。
- 安全认证与攻击防御刚起步，`UniTexture` 和 `DRIFT` 暴露 VLA 的脆弱性，但防御手段有限。
- 触觉数据获取成本高，`SoftVTBench` 显示触觉并非总是有效，如何高效利用触觉仍是开放问题。