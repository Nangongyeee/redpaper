# 2026-07 月度领域综述

_由 redpaper（deepseek-v4-flash）自动生成，覆盖 420 篇论文 / 视频；2026-07-31_

**一句话：** 世界模型与VLA深度融合，触觉与数据规模成焦点

## 一句话
世界模型与VLA深度融合，触觉与数据规模成焦点。

## 五大主题

### 世界模型从“看”到“做”：动作条件化与物理一致性
本月世界模型不再只是视频预测，而是与动作深度绑定，强调物理一致性和可规划性。`PhiZero` 用物理语言显式推理世界演化，再渲染成视频，实现物理一致的世界建模；`ShadowDancer` 通过阴影对和跨阴影预测，实现任意动作的精确控制，动作迁移显著提升。`KineBench` 则提出无IDM的闭环基准，通过运动学基准确立评估具身世界模型的物理一致性，直击世界模型在机器人任务中的评估痛点。这些工作表明，世界模型正从“生成视频”转向“生成可用的动作条件未来”，为规划和控制提供更可靠的想象空间。

### VLA模型走向实时与轻量化：推理加速与边缘部署
VLA模型在真实机器人上的部署瓶颈——推理延迟和显存占用——成为本月热点。`TurboVLA` 在RTX 4090上实现32 Hz实时推理，显存低于1 GB，通过直接映射视觉语言到动作；`Reflex` 通过时间步不变性和增量缓存实现流匹配VLA模型的实时推理，加速2.58倍。`Jetson-PI` 提出前瞻对齐异步推理，在Jetson Orin上实现机载实时控制。`ActionCache` 则通过缓存中间动作，实现流匹配VLA模型高达34倍推理加速。这些工作让VLA模型真正具备实时闭环能力，向实际部署迈出关键一步。

### 触觉感知成为操作智能的“新模态”：从传感器到世界模型
触觉不再只是辅助，而是与视觉并列的核心模态，甚至进入世界模型。`FasTac` 提出弯曲多光谱视觉触觉传感器，实现高精度三维重建和力估计；`ViTacWorld` 提出首个视触觉世界模型，利用仿真与真实数据生成轨迹并评估策略；`VT-WAM` 通过联合预测视觉和触觉形变，显著提升密集接触操作成功率。`TouchWorld` 提出分层触觉基础模型，分离预测与反应，提升灵巧操作成功率。触觉的加入让世界模型能处理接触丰富的任务，也推动了传感器硬件与算法的协同发展。

### 数据规模与质量并重：从人类视频到自动生成
数据仍是机器人学习的核心瓶颈，本月出现多种数据获取与利用的新思路。`ACE-Data-0` 提供大规模多模态同步人类演示数据；`Pegasus` 通过图结构知识转移将人类视频转化为机器人可学习数据；`PRISM` 从单张图像生成个性化机器人数据集，无需遥操作，成功率可达100%。`TableVerse` 从互联网图像自动重建高保真桌面场景，生成10万物理一致环境及操作轨迹。`AXIS` 则是一个社区驱动的数据引擎，支持浏览器远程收集数据。数据不再局限于遥操作，而是向自动化、规模化、多样化发展。

### 强化学习与VLA结合：后训练与测试时优化
VLA模型与强化学习的结合成为提升泛化能力的重要手段。`RL$^2$-VLA` 通过强化学习在VLA隐空间自适应组合引导，仅在预测失败时干预，提升域外任务成功率；`PAC-ACT` 提出训练后Actor-Critic方法，提升动作分块Transformer在工业接触任务中的性能；`ExToken` 通过条件化离散行为先验实现结构化探索，提升VLA模型强化微调的样本效率。`RedFlow` 将失败重定向为动作级修正，提升流匹配VLA策略的离线RL效率。这些工作表明，RL后训练正在成为VLA模型从“模仿”到“自适应”的关键环节。

## 值得抄作业的工作

- `FasTac`：值得抄的是弯曲多光谱视觉触觉传感器设计，低成本实现高精度三维形状与力感知，可直接用于接触丰富的操作任务。
- `TurboVLA`：值得抄的是直接映射视觉语言到动作的轻量架构，在RTX 4090上实现32 Hz实时推理，显存低于1 GB，适合边缘部署。
- `PRISM`：值得抄的是从单张图像生成个性化机器人数据集的流程，无需遥操作即可获得训练数据，成功率可达100%，极大降低数据成本。
- `KineBench`：值得抄的是无IDM的闭环基准设计，通过运动学基准确立评估具身世界模型的物理一致性，可复用于其他世界模型评估。
- `ViTacWorld`：值得抄的是视触觉世界模型的构建方法，利用仿真与真实数据生成轨迹并评估策略，为接触丰富任务提供新范式。

## 还没解决的事

- 世界模型的长时域稳定性：`Cycle-World` 和 `KineBench` 都指出误差累积问题，但尚无统一解决方案，长时域预测仍不可靠。
- VLA模型的因果推理能力：`HumanCLAW` 发现VLM缺乏具身自我意识，最佳成功率仅16.8%，`在视频中思考` 也指出视频生成器几乎无因果理解，如何注入因果推理仍是开放问题。
- 触觉数据的规模化：尽管有 `ViTacWorld` 和 `Deform360` 等数据集，但触觉数据采集成本高、标准化难，大规模触觉预训练尚未成熟。
- 跨形态迁移的泛化：`旧数据何时开始有用？` 发现旧数据在机器人升级后并非立即有用，需达到能力阈值才产生迁移收益，跨配置迁移的机制仍不清晰。
- 安全与鲁棒性：`推理是一把双刃剑` 揭示潜在迭代推理模型在扰动下最脆弱，`TrustVLA` 提出后门防御，但VLA模型在真实世界中的安全认证和鲁棒性保障仍缺乏系统性方案。